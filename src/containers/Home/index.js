import './style.css';
import React from 'react';
import Layout from '../../components/Layout';

const Home = (props) => {
    return (
        <Layout sidebar={true}>
        </Layout>
    );
};

export default Home;